/**
 * Delay between each API request to not overload the wiki
 * * DO NOT CHANGE THIS VALUE AT ALL
 * * The wiki folks are kind enough to let us scrape their data. Let's not abuse it.
 * * While this makes the script take a bit longer to run, it doesn't spike their cpu usage.
 */
export const REQUEST_DELAY = 2000
