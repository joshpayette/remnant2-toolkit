/**
 * The maximum amount of traits that can
 * be added to the builder.
 */
export const MAX_TRAIT_AMOUNT = 110;

/**
 * Traitor legendary fragment grants max core amount.
 * Each class gets 5 points spread across all 4 core amounts.
 * This means Traitor invests 35 points to max core amounts.
 */
const MAX_CORE_AMOUNT = 35;
export const MAX_TRAIT_AMOUNT_WITH_TRAITOR = MAX_TRAIT_AMOUNT + MAX_CORE_AMOUNT;
